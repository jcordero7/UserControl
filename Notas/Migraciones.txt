2:45 PM 9/30/2020
al hacer una migracion por primera vez de la base de datos
se tiene que invocar de la siguiente manera:
DESDE PMC

Add-Migration InitialCreate

Remove-Migration para eliminar una migracion


DESDE CMD
C:\Users\jcord\source\repos\SocialMedia\SocialMedia.Api>dotnet ef migrations add InitialCreate
Build started...
Build succeeded.
ERROR QUE DIO
Your target project 'SocialMedia.Api' doesn't match your migrations assembly 'SocialMedia.Infrastructure'. Either change your target project or change your migrations assembly.
Change your migrations assembly by using DbContextOptionsBuilder. E.g. options.UseSqlServer(connection, b => b.MigrationsAssembly("SocialMedia.Api")). By default, the migrations assembly is the assembly containing the DbContext.
Change your target project to the migrations project by using the Package Manager 
Console's Default project drop-down list, or by executing "dotnet ef" from the directory containing the migrations project.
